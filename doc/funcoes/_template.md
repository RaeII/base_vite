# Função/Hook — <nome>

> Copie para `doc/funcoes/<nome>.md`. Só para funções/hooks **globais**
> (`src/lib/` ou `src/hooks/`). Apague estas linhas depois.

[[index|← Índice]] · [[03-funcoes]]

**Arquivo:** `src/lib/<nome>.ts` _ou_ `src/hooks/<nome>.ts`
**Tipo:** função pura · hook

## O que faz

Uma a duas frases. Quando usar.

## Assinatura

```ts
function nome(param: Tipo): Retorno
```

| Parâmetro | Tipo | Descrição |
|---|---|---|
| `param` | `Tipo` | … |

**Retorno:** `Retorno` — …

## Uso

```ts
import { nome } from '../lib/nome'

const r = nome(valor)
```
